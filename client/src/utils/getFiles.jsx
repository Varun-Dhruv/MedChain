export const getFiles = (user) => {
    try {
        if (user === "Patient") {
            patient
        }
        else if (user === "Doctor") {

        }

    } catch (error) {
        console.error(error);
    }
}